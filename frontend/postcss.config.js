// Configuración de PostCSS para procesar CSS con plugins como Tailwind CSS y Autoprefixer.
export default {
  plugins: {
    // Integra Tailwind CSS. Asegúrate de que Tailwind CSS esté instalado.
    tailwindcss: {},
    // Añade prefijos de proveedor automáticamente a las propiedades CSS para compatibilidad con navegadores.
    autoprefixer: {},
  },
}
